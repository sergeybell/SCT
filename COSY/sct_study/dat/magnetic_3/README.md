# `dat/magnetic_3/`

Offline + validate + working points. **Трекинга нет** (пилот — только `magnetic_2`).

Типичный набор: `audit.json`, `zero_point.json`, `working_point.json`, `delta_eq_*.json`, `sext_phase.json`, `validate_*.dat`, `wp_*.dat`.
